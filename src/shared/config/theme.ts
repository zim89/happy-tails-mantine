import { createTheme, em } from '@mantine/core';

const theme = createTheme({
  white: '#fdfdfd',
  black: '#161616',
  colors: {},
  primaryShade: 4,
  fontFamily: 'Lato',
  breakpoints: {
    sm: '414px',
    md: '768px',
    lg: '1280px',
  },
});

export default theme;
